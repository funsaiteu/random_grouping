# READMEはとりあえずこれで許してください

```
% git clone git@github.com:funsaiteu/random_grouping.git
% cd random_grouping
% ./random_grouping.py -h
usage: random_grouping.py [-h] [-n N] [-m M] [-r R]

for online party(e.g. % ./random_grouping.py -n 3 -m ./member -r 0)

optional arguments:
  -h, --help  show this help message and exit
  -n N        number of split u wanna(default='3')
  -m M        member list's path(default='./member')
  -r R        random seed(default='0')
```
